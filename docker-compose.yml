version: '3.8'

services:
  trojans:
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: unless-stopped
    volumes:
      - ./server/data:/app/server/data
